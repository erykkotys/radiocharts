RadioCharts 0.3.18

- Wersja aplikacji jest stale widoczna małym drukiem w prawym górnym rogu, tuż obok menu Streamlita (⋮).
- Jeśli build Docker nie przekaże RADIOCHARTS_VERSION, aplikacja bierze wersję z pliku VERSION zamiast pokazywać "dev".
- SHA commita jest pokazywane obok wersji tylko wtedy, gdy build je faktycznie przekazał.
